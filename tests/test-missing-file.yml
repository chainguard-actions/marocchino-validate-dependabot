name: Test missing dependabot config file
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          path: '.github/dependabot-nonexistent.yml'
        continue-on-error: true

      - name: Assert action failed (missing file should fail)
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail when dependabot config file is missing"
            exit 1
          fi
          echo "PASS: action correctly failed for missing config file"
